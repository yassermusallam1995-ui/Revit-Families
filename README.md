# Revit Families Repository

This repository stores Revit family files (.rfa) and thumbnails uploaded via the RevitDrivePalette plugin.

## Structure
- Files are stored as GitHub Release assets
- `manifest_remote.json` contains the family metadata
- Each family has a thumbnail image and .rfa file

## Usage
Upload families using the RevitDrivePalette plugin in Revit 2024.
